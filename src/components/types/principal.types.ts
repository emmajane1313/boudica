export type PrincipalProps = {
  dict: Dictionary;
};

export type Dictionary = {
  Home: {
    gemara: string;
    toma: string;
    por: string;
    idioma: string;
    publicacion: string;
    billetera: string;
    lens: string;
  };
  "404": {
    nada: string;
  };
};
